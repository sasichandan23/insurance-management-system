# Copy to .env for local overrides, or set in the Vercel dashboard for production.
# Base URL of the backend REST API.
#   Local dev:  http://localhost:8081/api   (this is the default if unset)
#   Production: https://<your-render-backend>.onrender.com/api
VITE_API_BASE=http://localhost:8081/api
